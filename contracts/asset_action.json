{
  "asset_action": {
    "version": 1,
    "description": "Allows the model to run an asset action block.",
    "runtime_action": "ASSET_ACTION",
    "enable_flag": "CAN_USE_ASSETS",
    "runtime_order": 110,
    "private_marker": "<ASSET_ACTION>",
    "triggers": [],
    "blockers": [],
    "effects": {
      "emit_followup": true,
      "follow_up_on_fail": false
    },
    "schema": [],
    "rules": [
      "Must derive concrete ASSET_ACTION schema from LOADED_SKILLS_CONTENT block."
    ],
    "close_tag": true
  }
}
