{
  "asset_action": {
    "version": 1,
    "description": "Allows the model to run an asset action block.",
    "runtime_action": "ASSET_ACTION",
    "enable_flag": "CAN_USE_ASSETS",
    "runtime_order": 110,
    "private_marker": "<ASSET_ACTION>",
    "triggers": [],
    "blockers": [],
    "effects": {
      "emit_followup": true,
      "follow_up_on_fail": false
    },
    "rules": [
      "PROJECT ASSETS:",
      "Use only capabilities from skills already loaded in <LOADED_SKILLS_CONTENT>, and only when the user request requires them."
    ],
    "close_tag": true
  }
}
