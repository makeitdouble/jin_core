{
  "jin_color": {
    "version": 2,
    "description": "Allows the model to update the JIN runtime avatar center point color and its subtle glow for the current live session.",
    "runtime_action": "JIN_COLOR",
    "private_marker": "<JIN_COLOR>",
    "triggers": [],
    "blockers": [],
    "effects": {
      "emit_followup": false
    },
    "rules": [
      "Use to set single color for the live JIN avatar center point color and chat interface background tint.",
      "Emit marker using exactly this schema <JIN_COLOR> #00f2ff </JIN_COLOR>",
      "Put only the color payload inside the tag body; do not put it in the opening tag.",
      "Emit several markers in one message for consecutive color change or blinking effect, last color in sequence will be used as current.",
      "The payload must be a hex color with optional leading # and exactly 3 or 6 hex characters."
    ],
    "close_tag": true,
    "body_placeholder": "#00f2ff"
  }
}
