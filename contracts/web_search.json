{
  "web_search": {
    "version": 1,
    "description": "Allows the model to request a runtime web search.",
    "runtime_action": "WEB_SEARCH",
    "enable_flag": "CAN_WEB_SEARCH",
    "runtime_order": 20,
    "private_marker": "<WEB_SEARCH: plain text query >",
    "triggers": [],
    "blockers": [],
    "effects": {
      "emit_followup": true,
      "follow_up_on_fail": false
    },
    "schema": [],
    "rules": [
      "Use this marker for web searching via google. For casual searching must prefer this marker."
    ],
    "close_tag": false
  }
}
