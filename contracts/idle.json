{
  "idle": {
    "version": 1,
    "description": "Allows the model to schedule an idle follow-up tick.",
    "runtime_action": "IDLE",
    "private_marker": "<IDLE: Ns >",
    "triggers": [],
    "blockers": [],
    "effects": {
      "emit_followup": true
    },
    "rules": [
      "Emit to setup and trigger follow-up tick after Ns seconds. DO NOT USE in casual chat reminders."
    ],
    "close_tag": false
  }
}
