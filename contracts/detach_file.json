{
  "detach_file": {
    "version": 1,
    "description": "Removes one persistent file from the JIN context without deleting it.",
    "runtime_action": "DETACH_FILE",
    "private_marker": "<DETACH_FILE: id >",
    "triggers": [],
    "blockers": [],
    "effects": {"emit_followup": false},
    "rules": [
      "Emit <DETACH_FILE: id > to detach one file from context. The saved file remains in FILES storage.",
      "id is a placeholder; replace it with the 6-character id shown in <ATTACHED_FILES> or LIST_FILES."
    ],
    "close_tag": false
  }
}
