{
  "detach_file": {
    "version": 1,
    "description": "Unloads a file body from context without deleting its source.",
    "runtime_action": "DETACH_FILE",
    "enable_flag": "CAN_USE_ASSETS",
    "runtime_order": 140,
    "private_marker": "<DETACH_FILE: id >",
    "triggers": [],
    "blockers": [],
    "effects": {
      "emit_followup": true,
      "follow_up_on_fail": true
    },
    "schema": [
      "<DETACH_FILE: file_id >",
      "<DETACH_FILE: relative/path >",
      "<DETACH_FILE: folder_id/relative/path >"
    ],
    "rules": [
      "Use the same ID/path as ATTACH_FILE: relative to the single attached folder, or folder_id/path with multiple folders. Unloads content while preserving the action trail and source file; detaching a folder also unloads its files."
    ],
    "close_tag": false
  }
}
