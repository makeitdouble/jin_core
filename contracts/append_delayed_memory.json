{
  "append_delayed_memory": {
    "version": 1,
    "description": "Allows the model to append a delayed memory report into the session context.",
    "runtime_action": "APPEND_DELAYED_MEMORY",
    "private_marker": "<APPEND_DELAYED_MEMORY: id >",
    "triggers": [],
    "blockers": [],
    "effects": {
      "emit_followup": false
    },
    "rules": [
      "Emit one marker per saved delayed memory. You may emit multiple markers to append multiple reports; already appended ids are ignored.",
      "id - is a placeholder word; replace placeholder with the 6-character id prefix shown in <DELAYED_MEMORY>.",
      "Available for appending reports are listed in the <DELAYED_MEMORY> block.",
      "If a report already appended, use content placed in <APPENDED_DELAYED_MEMORY> block ."
    ],
    "close_tag": false
  }
}
